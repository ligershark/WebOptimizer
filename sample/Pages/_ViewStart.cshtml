@using WebOptimizer;
@{
    Layout = "_Layout";
    //Context.Response.Headers["Content-Security-Policy"] = "object-src 'none'; form-action 'self'; script-src 'unsafe-inline' 'nonce-" + Context.GetOrCreateNonce() + "' ";
}
